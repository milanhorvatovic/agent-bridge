{
  "$comment": "GENERATED FILE — do not edit by hand. Generated from the Rust types in crates/events by `cargo run -p agent-bridge-events --bin schema-gen`; CI regenerates this file and fails on any difference. The event taxonomy grows within schema_version 1 by additive, non-breaking changes only (new event types, new optional payload fields, new namespaces, new error codes under an existing error type), so consumers must ignore event types, payload fields, and error codes they do not recognize.",
  "$defs": {
    "AdapterErrorCode": {
      "anyOf": [
        {
          "const": "pattern_compile_failed",
          "description": "A pattern the adapter declared could not be compiled.",
          "type": "string"
        },
        {
          "const": "pattern_timeout",
          "description": "Matching took longer than the adapter's budget allows.",
          "type": "string"
        },
        {
          "const": "version_mismatch",
          "description": "The launched CLI's version is one the adapter cannot drive. The\nsofter case — a version outside the declared range that the adapter\nwill still attempt — is `adapter.version_warning`, not an error.",
          "type": "string"
        },
        {
          "description": "A code this revision does not know, carried verbatim. New codes are\nadditive, so a consumer must read them rather than reject them.",
          "type": "string"
        }
      ],
      "description": "The codes `adapter.error` publishes in this revision."
    },
    "AdapterErrorPayload": {
      "description": "Payload of `adapter.error` — the adapter hosting a CLI could not do its\njob.",
      "properties": {
        "code": {
          "$ref": "#/$defs/AdapterErrorCode",
          "description": "What went wrong, as a machine-readable code."
        },
        "detail": {
          "additionalProperties": true,
          "description": "Structured context for the code — the fields depend on which code it\nis. Absent when there is nothing to add.",
          "type": "object"
        },
        "message": {
          "description": "What went wrong, in a form a human can act on.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "AdapterVersionWarning": {
      "description": "Payload of `adapter.version_warning` — the launched CLI's version is\noutside the range its adapter declares support for.\n\nThe launch proceeds. The adapter's patterns were written against a version\nrange, so a version outside it is a reason to expect detection gaps —\nwhich surface as `stream.unrecognized_output` — not a reason to refuse to\nrun.",
      "properties": {
        "adapter": {
          "description": "The adapter that raised the warning (its source tag).",
          "type": [
            "string",
            "null"
          ]
        },
        "detected_version": {
          "description": "The CLI version that was launched, as the CLI reports it.",
          "type": [
            "string",
            "null"
          ]
        },
        "supported_range": {
          "description": "The version range the adapter declares support for.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CellColor": {
      "description": "A colour, in whichever of the two ways the CLI expressed it.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "An index into the terminal's palette, which resolves to a colour only\nonce a real terminal has a theme. Carried as the index rather than as\nthe colour it would resolve to, because the runtime has no theme and\nsubstituting one would be inventing information.",
          "properties": {
            "indexed": {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "indexed"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A direct colour: red, green, blue.",
          "properties": {
            "rgb": {
              "items": {
                "format": "uint8",
                "maximum": 255,
                "minimum": 0,
                "type": "integer"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            }
          },
          "required": [
            "rgb"
          ],
          "type": "object"
        }
      ]
    },
    "CellIntensity": {
      "description": "How heavily a cell is drawn.",
      "oneOf": [
        {
          "const": "normal",
          "description": "The default weight.",
          "type": "string"
        },
        {
          "const": "bold",
          "description": "SGR 1.",
          "type": "string"
        },
        {
          "const": "faint",
          "description": "SGR 2.",
          "type": "string"
        }
      ]
    },
    "CellStyle": {
      "description": "The display attributes of one cell — the terminal's SGR state where it\nwas written.\n\nEverything defaults to off, and everything off is omitted, so the common\ncell carries no style object at all.",
      "properties": {
        "background": {
          "anyOf": [
            {
              "$ref": "#/$defs/CellColor"
            },
            {
              "type": "null"
            }
          ],
          "description": "Background colour, or `null` for the terminal's default."
        },
        "blink": {
          "description": "Blinking.",
          "type": "boolean"
        },
        "foreground": {
          "anyOf": [
            {
              "$ref": "#/$defs/CellColor"
            },
            {
              "type": "null"
            }
          ],
          "description": "Text colour, or `null` for the terminal's default."
        },
        "intensity": {
          "$ref": "#/$defs/CellIntensity",
          "description": "Bold, faint, or neither. One field rather than two flags because a\nterminal is in exactly one of the three states."
        },
        "inverse": {
          "description": "Foreground and background swapped.",
          "type": "boolean"
        },
        "italic": {
          "description": "Italic.",
          "type": "boolean"
        },
        "strikethrough": {
          "description": "Struck through.",
          "type": "boolean"
        },
        "underline": {
          "description": "Underlined.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CursorPosition": {
      "description": "A cursor position on the reconstructed screen, zero-based from the top\nleft.\n\nWhere the terminal would put the caret, and only that. A CLI that hides\nthe cursor while it paints still has one somewhere, and this reports where\n— so a snapshot taken mid-paint is indistinguishable from one where the\ncursor is on screen.\n\n**Visibility is deliberately absent, on measurement rather than on\nprinciple.** The obvious use for it is liveness — is the interface still\ndrawing — and a snapshot is the wrong shape to answer that. Recorded\nsessions hide and show the cursor 20 to 99 times each, once around every\nrepaint, so what carries the signal is the *transitions*, which a\nsingle-moment snapshot cannot hold. Sampled at evaluation points the same\nrecordings read 541 visible against 92 hidden, and those 92 are points\nthat happened to land mid-repaint — a property of when the sample fell,\nnot of the session. A consumer reading `hidden` as meaningful would be\nreading sampling noise.\n\nLiveness is already answerable without a new field: a screen that is\nbeing painted reports damaged rows, and one that is not reports none.\nAdding visibility here stays possible as a new optional field if some\nconsumer turns out to want the state rather than the signal.",
      "properties": {
        "col": {
          "description": "Column, from the left.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "row": {
          "description": "Row, from the top.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "row",
        "col"
      ],
      "type": "object"
    },
    "HealthStatus": {
      "description": "How the runtime assesses itself.",
      "oneOf": [
        {
          "const": "ok",
          "description": "Serving normally.",
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "Serving, with something worth knowing about — the runtime keeps\nhosting sessions but some capability is impaired.",
          "type": "string"
        },
        {
          "const": "unhealthy",
          "description": "Not serving reliably.",
          "type": "string"
        }
      ]
    },
    "LifecycleSessionAwaitingApproval": {
      "description": "Payload of `lifecycle.session.awaiting_approval` — the session is blocked\non at least one human decision.\n\nPaired with `prompt.approval_required`, which carries the prompt itself\nand the `approval_id` that resolves it. This event is the *state*\ntransition and is deliberately not correlated to any one approval: a\nsession can hold several pending approvals at once, so its envelope\n`approval_id` stays `null` and the prompt events carry the ids.",
      "type": "object"
    },
    "LifecycleSessionClosed": {
      "description": "Payload of `lifecycle.session.closed` — the session has ended, with what\nis known about how it ended.\n\nEvery field is optional because what is known depends on how the session\nended: a CLI killed after a drain timeout has no exit code to report, and\na session that failed before launch has no byte counts.",
      "properties": {
        "bytes_read": {
          "description": "Bytes read from the CLI over the session's lifetime, before any\nterminal-control stripping — what the process actually produced.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "bytes_written": {
          "description": "Bytes written to the CLI's input over the session's lifetime.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cleanup_verified": {
          "description": "Whether the close's containment census proved the process tree\nempty: `true` when nothing remained, `false` when the close\nfinished with the verification unsatisfied — processes still\nvisible past the termination sequence, or a census the operating\nsystem would not answer. Absent when no verification ever ran (a\nsession that failed before a terminal stood). A `false` is always\npaired with a loud runtime-log record; reclaiming what survives\nbelongs to supervision.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "drained": {
          "description": "Whether the shutdown hint worked: `true` when the CLI exited on\nits own during the graceful close — while the hint was still being\ndelivered, or within the drain window that follows it — and\n`false` when the close escalated to termination before a voluntary\nexit: because the window expired, or because a force-close cut the\nwait short (whether the window was armed yet or not), and trailing\noutput may then be missing. Absent when there was never a graceful\nclose to answer for: a close that was forced from the start, or a\nsession that ended by failing.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "duration_ms": {
          "description": "How long the session lived, in milliseconds, from create to close.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "exit_code": {
          "description": "The CLI process's exit code, when it exited with one.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "remaining_processes": {
          "description": "How many processes the final census still saw, when it could count\nthem. Present only beside `cleanup_verified: false`.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "LifecycleSessionClosing": {
      "description": "Payload of `lifecycle.session.closing` — termination has been initiated.\nNo fields yet; fields arrive additively.",
      "type": "object"
    },
    "LifecycleSessionCompacting": {
      "description": "Payload of `lifecycle.session.compacting` — the CLI is compacting its own\ncontext.\n\nAdvisory: the session stays live and the runtime keeps following the same\noutput. It is published because a caller watching for a silent stretch\notherwise has no way to tell compaction from a stall. No fields yet;\nfields arrive additively.",
      "type": "object"
    },
    "LifecycleSessionConnecting": {
      "description": "Payload of `lifecycle.session.connecting` — the CLI process is alive; no\noutput has been observed yet. No fields yet; fields arrive additively.",
      "type": "object"
    },
    "LifecycleSessionCreated": {
      "description": "Payload of `lifecycle.session.created` — the registry entry exists; no\nterminal has been allocated yet.",
      "properties": {
        "adapter": {
          "description": "Name of the adapter hosting the session (its source tag).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "LifecycleSessionInterrupted": {
      "description": "Payload of `lifecycle.session.interrupted` — an interrupt was forwarded\nto the CLI and the CLI acknowledged it. Every approval the session was\nholding is cancelled by the same interrupt. No fields yet; fields arrive\nadditively.",
      "type": "object"
    },
    "LifecycleSessionLaunching": {
      "description": "Payload of `lifecycle.session.launching` — the terminal is allocated and\nthe CLI process is being started. No fields yet; fields arrive additively.",
      "type": "object"
    },
    "LifecycleSessionRunning": {
      "description": "Payload of `lifecycle.session.running` — first output observed; the\nsession is live. No fields yet; fields arrive additively.",
      "type": "object"
    },
    "LifecycleTurnCompleted": {
      "description": "Payload of `lifecycle.turn.completed` — an assistant turn ended.\n\nThe turn finished on its own. A turn cut short by an interrupt is\n`lifecycle.session.interrupted` instead — the two are different outcomes\nand callers routinely need to tell them apart. No fields yet; fields\narrive additively.",
      "type": "object"
    },
    "LifecycleTurnStarted": {
      "description": "Payload of `lifecycle.turn.started` — an assistant turn began.\n\nA turn is one caller prompt and the work the CLI does in response. The\nboundary is reported by the CLI's structured channel where it has one;\nadapters without such a channel do not emit turn events at all, which is\nwhy consumers must treat them as informative rather than as a frame they\ncan require around every token. No fields yet; fields arrive additively.",
      "type": "object"
    },
    "PromptApprovalRequired": {
      "description": "Payload of `prompt.approval_required` — the CLI is waiting on a decision\nonly a human can make.\n\nThe envelope's `approval_id` is required on this event and on no other:\nit is what the caller answers with, and a prompt nobody can answer leaves\nthe CLI blocked. Events that are not about one specific pending approval\ncarry `null` there, even while approvals are pending.",
      "properties": {
        "options": {
          "description": "The answer options the CLI offers, when they are known.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "prompt": {
          "description": "The prompt text presented by the CLI.",
          "type": "string"
        },
        "tool": {
          "description": "The tool the decision is about (for example `\"bash\"`), when the CLI\nnames one.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "PtyErrorCode": {
      "anyOf": [
        {
          "const": "pty_alloc_failed",
          "description": "A pseudo-terminal could not be allocated for the session.",
          "type": "string"
        },
        {
          "const": "stdin_blocked",
          "description": "A write to the CLI's input blocked long enough to report.",
          "type": "string"
        },
        {
          "const": "encoding_replacement",
          "description": "Undecodable bytes were replaced to keep the stream flowing — output\nis preserved, but altered.",
          "type": "string"
        },
        {
          "const": "encoding_burst",
          "description": "Replacements arrived in a burst, which usually means the CLI is\nemitting something that is not text at all.",
          "type": "string"
        },
        {
          "const": "child_exec_failed",
          "description": "The CLI process could not be started in the allocated terminal.",
          "type": "string"
        },
        {
          "const": "child_exited_early",
          "description": "The operation could not be carried out because the CLI process is\nalready gone. Distinct from the terminal failing: nothing is wrong\nwith it, there is simply nobody left in it.",
          "type": "string"
        },
        {
          "const": "signal_delivery_failed",
          "description": "A signal could not be delivered to the CLI's process group. Also how\na platform reports a signal it has no equivalent for, because the\nhonest answer is that the delivery did not happen.",
          "type": "string"
        },
        {
          "const": "early_resize",
          "description": "The terminal was resized before the CLI had taken possession of it.\nThe geometry was applied; what is uncertain is whether anything was\nthere to be notified, so the resize is worth reissuing.",
          "type": "string"
        },
        {
          "const": "pty_io_failed",
          "description": "The terminal itself failed an operation, for a reason that is not the\nCLI having exited. A session cannot continue on it.",
          "type": "string"
        },
        {
          "description": "A code this revision does not know, carried verbatim. New codes are\nadditive, so a consumer must read them rather than reject them.",
          "type": "string"
        }
      ],
      "description": "The codes `pty.error` publishes in this revision."
    },
    "PtyErrorPayload": {
      "description": "Payload of `pty.error` — the terminal, or the process hosted in it,\nfailed.",
      "properties": {
        "code": {
          "$ref": "#/$defs/PtyErrorCode",
          "description": "What went wrong, as a machine-readable code."
        },
        "detail": {
          "additionalProperties": true,
          "description": "Structured context for the code — the fields depend on which code it\nis. Absent when there is nothing to add.",
          "type": "object"
        },
        "message": {
          "description": "What went wrong, in a form a human can act on.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "ReplayInfo": {
      "description": "The outcome of a backfill request: how much history the subscriber got\nback, and whether any was already gone.\n\nThree outcomes, three shapes: the requested position was still buffered\nand everything since it was replayed; it had already been evicted and\nnothing was; or no backfill was asked for.\n\nThe gap case is the one that matters: history is bounded, so a subscriber\nthat was away too long cannot be given what it missed. Saying so\nexplicitly — with the earliest sequence number still available, and a\nscreen snapshot where the adapter keeps one — lets the subscriber resync\ndeliberately instead of silently believing it has the whole stream.",
      "properties": {
        "earliest_seq": {
          "description": "The oldest sequence number still buffered — or, when nothing is\nbuffered at all, the head position the next event will occupy.\nPresent on a gap, where it tells the subscriber how much it lost:\neverything below this value is gone, and resuming from it (or any\nlater position) is well-defined.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "events_replayed": {
          "description": "How many buffered events were delivered before the live stream\nresumed.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "gap": {
          "description": "`true` when the requested position had already been evicted, so some\nevents can never be delivered.",
          "type": "boolean"
        },
        "replayed_from": {
          "description": "The sequence number replay actually started from, or `null` when\nnothing was replayed.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "screen_snapshot": {
          "anyOf": [
            {
              "$ref": "#/$defs/ScreenSnapshot"
            },
            {
              "type": "null"
            }
          ],
          "description": "The session's reconstructed screen at re-attach time, for adapters\nthat keep one. Present on a gap, where it is the only way to convey\nstate the evicted events carried."
        }
      },
      "required": [
        "events_replayed",
        "gap"
      ],
      "type": "object"
    },
    "RuntimeErrorCode": {
      "anyOf": [
        {
          "const": "supervisor_force_close",
          "description": "A session did not stop when asked and the supervisor closed it by\nforce.",
          "type": "string"
        },
        {
          "const": "registry_race",
          "description": "Two operations raced over the same session registry entry.",
          "type": "string"
        },
        {
          "const": "log_disk_full",
          "description": "Logs cannot be written because the disk is full. The runtime keeps\nserving sessions; the diagnostic record is what is lost, and saying\nso is the point of the event.",
          "type": "string"
        },
        {
          "const": "log_rotation_failed",
          "description": "A log file could not be rotated.",
          "type": "string"
        },
        {
          "const": "config_invalid",
          "description": "Configuration was rejected as invalid.",
          "type": "string"
        },
        {
          "description": "A code this revision does not know, carried verbatim. New codes are\nadditive, so a consumer must read them rather than reject them.",
          "type": "string"
        }
      ],
      "description": "The codes `runtime.error` publishes in this revision."
    },
    "RuntimeErrorPayload": {
      "description": "Payload of `runtime.error` — the runtime core itself hit a condition it\nhas to report.",
      "properties": {
        "code": {
          "$ref": "#/$defs/RuntimeErrorCode",
          "description": "What went wrong, as a machine-readable code."
        },
        "detail": {
          "additionalProperties": true,
          "description": "Structured context for the code — the fields depend on which code it\nis. Absent when there is nothing to add.",
          "type": "object"
        },
        "message": {
          "description": "What went wrong, in a form a human can act on.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "RuntimeHealthChanged": {
      "description": "Payload of `runtime.health_changed` — the runtime's health assessment\nmoved.\n\nCallers poll health when they want a snapshot; they subscribe to this when\nthey want to hear about the transition without polling for it.",
      "properties": {
        "previous": {
          "anyOf": [
            {
              "$ref": "#/$defs/HealthStatus"
            },
            {
              "type": "null"
            }
          ],
          "description": "The health it reported before this transition."
        },
        "reason": {
          "description": "What moved it, in a form a human can act on (for example\n`\"log disk below 5% free\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/HealthStatus",
          "description": "The health the runtime reports now."
        }
      },
      "required": [
        "status"
      ],
      "type": "object"
    },
    "RuntimeIdleTooLong": {
      "description": "Payload of `runtime.idle_too_long` — a running session has produced no\nevent for the configured threshold.\n\nThe session is untouched: this is the runtime saying \"nothing has happened\nfor a while\", which a caller may read as a stuck CLI, a long-running tool,\nor a human staring at a prompt. Deciding which is the caller's business.",
      "properties": {
        "idle_ms": {
          "description": "How long the session has been silent, in milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "threshold_ms": {
          "description": "The configured threshold that fired, in milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RuntimeNotice": {
      "description": "Payload of `runtime.notice` — a notification the hosted CLI raised\nthrough a structured channel, passed through with its kind named.\n\nThis is the catch-all for the notifications a CLI emits that are neither\noutput nor lifecycle: a permission dialog appearing, an idle nudge. The\nruntime does not interpret them, because interpreting them would mean\ninventing a taxonomy for every CLI's notification vocabulary; it names the\nkind and carries the rest verbatim.",
      "properties": {
        "detail": {
          "additionalProperties": true,
          "description": "Whatever else the notification carried, passed through unmodified.\nAbsent when it carried nothing else.",
          "type": "object"
        },
        "message": {
          "description": "The notification's human-readable text, when it has one.",
          "type": [
            "string",
            "null"
          ]
        },
        "notification_type": {
          "description": "The kind of notification, in the CLI's own vocabulary (for example\n`\"permission_prompt\"` or `\"idle_prompt\"`).",
          "type": "string"
        }
      },
      "required": [
        "notification_type"
      ],
      "type": "object"
    },
    "ScreenCell": {
      "description": "One cell of the reconstructed screen: the character it shows, and how it\nis drawn.\n\nEvery cell is the same shape, and the two fields that are usually\nuninteresting are omitted when they are — a plain character in the\ndefault style serializes as `{\"ch\": \"x\"}`. The alternative encodings\nweighed against this one were a bare string per plain cell and\nrun-length rows; both are smaller, and both cost the property that makes\nthis one worth the bytes, which is that `cells[row][col]` is the cell at\nthat column and a consumer needs no second code path to read it.",
      "properties": {
        "ch": {
          "description": "The character in the cell.\n\nOne Unicode scalar, which is what the emulator places in a column —\nand not always what a reader would call one character.\n\n**To read a row as text, skip the cells with `width` 0** and\nconcatenate the rest. The covered half of a double-width glyph is a\nspace with `width` 0, so concatenating every `ch` turns `漢x` into\n`漢 x`.\n\n**Column geometry is the emulator's, and it diverges from a real\nterminal on combining marks.** A terminal composes a combining mark\ninto the cell before it, showing `é` in one column; this emulator\ngives the mark a column of its own, so a decomposed letter takes two\ncells and everything after it sits one column further right than it\nwould on screen. Joined emoji sequences shift further still. Text\nread as above is unaffected — the scalars concatenate correctly — but\nanything addressing a *column* is reading the emulator's geometry,\nwhich equals the display's only while the row holds no zero-width\nscalars.",
          "maxLength": 1,
          "minLength": 1,
          "type": "string"
        },
        "style": {
          "description": "How the cell is drawn, as a position in the snapshot's\n[`styles`](ScreenSnapshot::styles). Omitted at `0`, which is the\ndefault style.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "width": {
          "description": "How many columns the character occupies: `1` normally, `2` for the\nleading half of a double-width glyph, and `0` for the column that\nhalf covers — which is carried as its own cell so that a column index\nstill addresses a column. Omitted at the usual `1`.\n\nThose three are the whole domain, and the published schema says so\nrather than leaving the byte's full range valid: a document carrying\na width of 47 would otherwise validate, and a consumer would have to\ninvent a meaning for it.",
          "format": "uint8",
          "maximum": 2,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "ch"
      ],
      "type": "object"
    },
    "ScreenSnapshot": {
      "description": "The session's reconstructed screen: the characters a terminal attached to\nthe CLI would be showing, how each is drawn, and where the cursor sits.\n\nNot a pixel-faithful account of the terminal, and it does not try to be.\nWhat a renderer would additionally need — whether the cursor is currently\ndrawn or hidden, its shape, whether the screen is in an alternate buffer —\nis absent, because what reads a snapshot is a matcher looking for text and\na caller catching up after a gap in its history. Anything from that list\ncan be added later without breaking a reader, being new optional fields.",
      "properties": {
        "cells": {
          "description": "The screen contents, row-major: `cells[row][col]`.\n\nThere is one entry per row — a blank row is an empty array rather\nthan an absent one, so a row index means the same thing on every\nsnapshot. Within a row the trailing blank cells are dropped, so a row\nis at most `cols` long and usually far shorter: a column past the end\nof a row is blank in the default style.\n\n**Both of those are promises about what this runtime emits, not\nthings the schema can enforce**, for the same reason the style index\nis not: JSON Schema cannot say that one field's length matches\nanother's value. A document can be schema-valid with more rows than\n`rows` or a row longer than `cols`, so bounds-check anything you did\nnot produce rather than indexing on the guarantee. That is what keeps a\nfull-screen snapshot proportional to what is written on the screen\nrather than to its area, which matters because a snapshot travels\nwhole and a mostly-empty screen is the normal case.",
          "items": {
            "items": {
              "$ref": "#/$defs/ScreenCell"
            },
            "type": "array"
          },
          "type": "array"
        },
        "cols": {
          "description": "Screen width in columns.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "cursor": {
          "$ref": "#/$defs/CursorPosition",
          "description": "Where the cursor is."
        },
        "rows": {
          "description": "Screen height in rows.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "styles": {
          "description": "Every way a cell on this screen is drawn, each listed once. Never\nempty: index 0 is the default style on every snapshot, used or not.\n\nCells name a style by its position here rather than carrying one, so\nreading a cell's style is `styles[cell.style]`. A consumer that only\nwants the text never touches this at all.\n\n**Bounds-check that lookup on any document you did not produce.**\nEvery snapshot this runtime emits names a style that exists, and the\nnon-empty guarantee above is in the published schema — but JSON\nSchema cannot say \"this index is within that array\", so a document\ncan be schema-valid and still name style 9 out of a list of two.\nTreat an out-of-range index as the default style rather than trusting\nthe pairing.\n\nThe indirection is here because it is where nearly all the size is. A\nterminal interface draws a whole screen out of a handful of colours —\nmeasured across the recorded sessions, four to fifteen distinct styles\ncovering one to two thousand cells — so a style written into every\ncell that uses it is the same short object repeated a thousand times.\nNaming them instead halves a snapshot.",
          "items": {
            "$ref": "#/$defs/CellStyle"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "cols",
        "rows",
        "cursor",
        "styles",
        "cells"
      ],
      "type": "object"
    },
    "SessionReconnected": {
      "description": "Payload of `session.reconnected` — the re-attach completed, with what the\nsubscriber did or did not get back.",
      "properties": {
        "replay": {
          "$ref": "#/$defs/ReplayInfo",
          "description": "What backfill delivered."
        }
      },
      "required": [
        "replay"
      ],
      "type": "object"
    },
    "SessionReconnecting": {
      "description": "Payload of `session.reconnecting` — a subscriber is re-attaching to a\nlive session.",
      "properties": {
        "from_seq": {
          "description": "The sequence number the subscriber asked to resume from, or `null`\nwhen it asked for the live stream without backfill.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "subscriber": {
          "description": "The re-attaching subscriber.",
          "type": "string"
        }
      },
      "required": [
        "subscriber"
      ],
      "type": "object"
    },
    "SessionWriterChanged": {
      "description": "Payload of `session.writer_changed` — which subscriber may write to the\nsession changed hands.\n\nPublished so every reader can reflect the current writer, including the\ncase where there now is none. The type is part of the contract from the\nstart even though nothing emits it yet: a runtime serving one caller has\nno writer to transfer, and the callers that will need this — several\nclients sharing one session — should be able to write against the shape\nbefore it starts arriving.",
      "properties": {
        "previous_writer": {
          "description": "The subscriber that could write before, or `null` when nobody could.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "$ref": "#/$defs/WriterChangeReason",
          "description": "Why ownership moved."
        },
        "writer": {
          "description": "The subscriber that may now write, or `null` when nobody may.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "reason"
      ],
      "type": "object"
    },
    "StreamStderr": {
      "description": "Payload of `stream.stderr` — an error-channel line after segmentation.\n\nBest-effort by nature: a terminal merges the CLI's error output into its\nstandard output, so this event fires only where the adapter declares a\nway to tell them apart. Without one, error text arrives as\n[`StreamToken`] instead. A caller that needs a guaranteed error channel\nuses the `transport.error` / `pty.error` / `adapter.error` /\n`runtime.error` events, which are always emitted as such.",
      "properties": {
        "content": {
          "description": "The error-channel text, after terminal-control stripping.",
          "type": "string"
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    },
    "StreamToken": {
      "description": "Payload of `stream.token` — incremental output: plain text after\nterminal-control stripping.",
      "properties": {
        "content": {
          "description": "The text chunk, after terminal-control stripping.",
          "type": "string"
        },
        "source": {
          "description": "The emitting adapter's source tag (for example `\"claude\"`), so\nconsumers can filter when sessions of different CLIs are active.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    },
    "StreamUnrecognizedOutput": {
      "description": "Payload of `stream.unrecognized_output` — output the runtime could not\nclassify.\n\nInstead of dropping it, the runtime degrades to \"here is the text\" — the\nsingle most important resilience event: when an adapter's patterns miss a\nCLI update, consumers still see everything, classified or not.",
      "properties": {
        "content": {
          "description": "The unclassified text, after terminal-control stripping, so the\nconsumer can decide what to make of it.",
          "type": "string"
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    },
    "ToolCallCompleted": {
      "description": "Payload of `tool.call_completed` — the tool invocation finished.",
      "properties": {
        "call_id": {
          "description": "The `call_id` of the [`ToolCallStarted`] this closes.",
          "type": "string"
        },
        "duration_ms": {
          "description": "How long the call took, in milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "exit_code": {
          "description": "The tool's exit code, for tools that have one.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "call_id"
      ],
      "type": "object"
    },
    "ToolCallFailed": {
      "description": "Payload of `tool.call_failed` — the tool invocation did not finish.",
      "properties": {
        "call_id": {
          "description": "The `call_id` of the [`ToolCallStarted`] this closes.",
          "type": "string"
        },
        "reason": {
          "description": "Why the call failed, in the CLI's own terms (for example\n`\"timeout\"`). Free text: what a CLI can report about a tool failure\nis not a set this runtime can close.",
          "type": "string"
        }
      },
      "required": [
        "call_id",
        "reason"
      ],
      "type": "object"
    },
    "ToolCallStarted": {
      "description": "Payload of `tool.call_started` — the CLI began invoking a tool.",
      "properties": {
        "call_id": {
          "description": "Opaque identifier pairing this call's start, end, and result. Where\nthe CLI supplies its own call identifier the runtime uses it verbatim\nrather than synthesizing a second one.",
          "type": "string"
        },
        "command": {
          "description": "The concrete invocation, for tools that have one to show (a shell\ncommand line, a file path). Absent where the tool's input is\nstructured or too large to inline.",
          "type": [
            "string",
            "null"
          ]
        },
        "tool": {
          "description": "The tool being invoked, as the CLI names it (for example `\"bash\"`).",
          "type": "string"
        }
      },
      "required": [
        "call_id",
        "tool"
      ],
      "type": "object"
    },
    "ToolResult": {
      "description": "Payload of `tool.result` — the tool's own output, as its own event.\n\nSeparate from the call lifecycle because some adapters surface a tool's\ntextual output as a distinct record while others interleave it into\n`stream.token`. A consumer that wants tool output without the surrounding\nnarration subscribes to this type; one that wants the session as it read\non screen subscribes to the token stream.",
      "properties": {
        "call_id": {
          "description": "The `call_id` of the [`ToolCallStarted`] this output belongs to.",
          "type": "string"
        },
        "content": {
          "description": "The tool's output text.",
          "type": "string"
        }
      },
      "required": [
        "call_id",
        "content"
      ],
      "type": "object"
    },
    "TransportErrorCode": {
      "anyOf": [
        {
          "const": "frame_too_large",
          "description": "A frame exceeded the transport's size cap.",
          "type": "string"
        },
        {
          "const": "malformed_frame",
          "description": "A frame could not be parsed as the protocol requires.",
          "type": "string"
        },
        {
          "const": "subscriber_lagging",
          "description": "A subscriber fell far enough behind that the runtime disconnected it\nrather than let its backlog grow without bound. The session itself\ncontinues.",
          "type": "string"
        },
        {
          "const": "stdout_blocked",
          "description": "The caller stopped reading the runtime's output. There is no recovery\nfrom a parent that does not read: the runtime reports this and exits.",
          "type": "string"
        },
        {
          "description": "A code this revision does not know, carried verbatim. New codes are\nadditive, so a consumer must read them rather than reject them.",
          "type": "string"
        }
      ],
      "description": "The codes `transport.error` publishes in this revision."
    },
    "TransportErrorPayload": {
      "description": "Payload of `transport.error` — the JSON-RPC wire could not carry\nsomething, or a subscriber could not keep up with it.",
      "properties": {
        "code": {
          "$ref": "#/$defs/TransportErrorCode",
          "description": "What went wrong, as a machine-readable code."
        },
        "detail": {
          "additionalProperties": true,
          "description": "Structured context for the code — the fields depend on which code it\nis. Absent when there is nothing to add.",
          "type": "object"
        },
        "message": {
          "description": "What went wrong, in a form a human can act on.",
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "WriterChangeReason": {
      "description": "Why write ownership of a session moved.",
      "oneOf": [
        {
          "const": "acquire",
          "description": "A subscriber claimed it.",
          "type": "string"
        },
        {
          "const": "release",
          "description": "The writer gave it up.",
          "type": "string"
        },
        {
          "const": "transport_drop",
          "description": "The writer's connection went away and the runtime released it on the\nwriter's behalf.",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "allOf": [
    {
      "description": "The session exists in the runtime's registry; no terminal has been\nallocated yet.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.created"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionCreated"
          }
        }
      }
    },
    {
      "description": "The terminal is allocated and the CLI process is being started.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.launching"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionLaunching"
          }
        }
      }
    },
    {
      "description": "The CLI process is alive; no output has been observed yet.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.connecting"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionConnecting"
          }
        }
      }
    },
    {
      "description": "First output observed; the session is live.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.running"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionRunning"
          }
        }
      }
    },
    {
      "description": "The session is blocked on at least one human decision. The prompts\nthemselves arrive as `prompt.approval_required`.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.awaiting_approval"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionAwaitingApproval"
          }
        }
      }
    },
    {
      "description": "An interrupt was forwarded to the CLI and the CLI acknowledged it.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.interrupted"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionInterrupted"
          }
        }
      }
    },
    {
      "description": "Termination has been initiated.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.closing"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionClosing"
          }
        }
      }
    },
    {
      "description": "The session has ended.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.closed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionClosed"
          }
        }
      }
    },
    {
      "description": "The CLI is compacting its own context; the session stays live.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.session.compacting"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleSessionCompacting"
          }
        }
      }
    },
    {
      "description": "An assistant turn began.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.turn.started"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleTurnStarted"
          }
        }
      }
    },
    {
      "description": "An assistant turn ended on its own.",
      "if": {
        "properties": {
          "type": {
            "const": "lifecycle.turn.completed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/LifecycleTurnCompleted"
          }
        }
      }
    },
    {
      "description": "Incremental output: plain text after terminal-control stripping.",
      "if": {
        "properties": {
          "type": {
            "const": "stream.token"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/StreamToken"
          }
        }
      }
    },
    {
      "description": "An error-channel line, for adapters that can tell one apart from\nordinary output.",
      "if": {
        "properties": {
          "type": {
            "const": "stream.stderr"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/StreamStderr"
          }
        }
      }
    },
    {
      "description": "Output the runtime could not classify. Instead of dropping it, the\nruntime degrades to \"here is the text\" — the single most important\nresilience event: consumers see everything, classified or not.",
      "if": {
        "properties": {
          "type": {
            "const": "stream.unrecognized_output"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/StreamUnrecognizedOutput"
          }
        }
      }
    },
    {
      "description": "The CLI is blocked on a human decision. The envelope's `approval_id`\nis non-null on this event and correlates the eventual resolution.",
      "if": {
        "properties": {
          "type": {
            "const": "prompt.approval_required"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "approval_id": {
            "type": "string"
          },
          "payload": {
            "$ref": "#/$defs/PromptApprovalRequired"
          }
        },
        "required": [
          "approval_id"
        ]
      }
    },
    {
      "description": "The CLI began invoking a tool.",
      "if": {
        "properties": {
          "type": {
            "const": "tool.call_started"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/ToolCallStarted"
          }
        }
      }
    },
    {
      "description": "A tool invocation finished.",
      "if": {
        "properties": {
          "type": {
            "const": "tool.call_completed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/ToolCallCompleted"
          }
        }
      }
    },
    {
      "description": "A tool invocation did not finish.",
      "if": {
        "properties": {
          "type": {
            "const": "tool.call_failed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/ToolCallFailed"
          }
        }
      }
    },
    {
      "description": "A tool's own output, surfaced as its own event.",
      "if": {
        "properties": {
          "type": {
            "const": "tool.result"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/ToolResult"
          }
        }
      }
    },
    {
      "description": "The runtime core hit a condition it has to report.",
      "if": {
        "properties": {
          "type": {
            "const": "runtime.error"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/RuntimeErrorPayload"
          }
        }
      }
    },
    {
      "description": "A running session has been silent for longer than the configured\nthreshold.",
      "if": {
        "properties": {
          "type": {
            "const": "runtime.idle_too_long"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/RuntimeIdleTooLong"
          }
        }
      }
    },
    {
      "description": "The runtime's health assessment moved.",
      "if": {
        "properties": {
          "type": {
            "const": "runtime.health_changed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/RuntimeHealthChanged"
          }
        }
      }
    },
    {
      "description": "A notification the hosted CLI raised through a structured channel.",
      "if": {
        "properties": {
          "type": {
            "const": "runtime.notice"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/RuntimeNotice"
          }
        }
      }
    },
    {
      "description": "The JSON-RPC wire could not carry something, or a subscriber could\nnot keep up with it.",
      "if": {
        "properties": {
          "type": {
            "const": "transport.error"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/TransportErrorPayload"
          }
        }
      }
    },
    {
      "description": "The terminal, or the process hosted in it, failed.",
      "if": {
        "properties": {
          "type": {
            "const": "pty.error"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/PtyErrorPayload"
          }
        }
      }
    },
    {
      "description": "The adapter hosting a CLI could not do its job.",
      "if": {
        "properties": {
          "type": {
            "const": "adapter.error"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/AdapterErrorPayload"
          }
        }
      }
    },
    {
      "description": "The launched CLI's version is outside the range its adapter declares\nsupport for. The launch proceeds.",
      "if": {
        "properties": {
          "type": {
            "const": "adapter.version_warning"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/AdapterVersionWarning"
          }
        }
      }
    },
    {
      "description": "A subscriber is re-attaching to a live session.",
      "if": {
        "properties": {
          "type": {
            "const": "session.reconnecting"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/SessionReconnecting"
          }
        }
      }
    },
    {
      "description": "The re-attach completed, reporting what backfill delivered.",
      "if": {
        "properties": {
          "type": {
            "const": "session.reconnected"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/SessionReconnected"
          }
        }
      }
    },
    {
      "description": "Write ownership of a session moved.",
      "if": {
        "properties": {
          "type": {
            "const": "session.writer_changed"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "payload": {
            "$ref": "#/$defs/SessionWriterChanged"
          }
        }
      }
    }
  ],
  "description": "One structured event emitted by the runtime.\n\nEvery event shares this envelope; the `type` discriminant names the\nevent and `payload` carries its type-specific fields. This revision\ndescribes `schema_version` 1; the taxonomy grows within it by additive,\nnon-breaking changes only, so a consumer must ignore unknown event types\nand unknown fields rather than reject them.",
  "properties": {
    "approval_id": {
      "description": "Correlates the event with one specific pending approval. Carried\n(non-null) only on events tied to that approval — required on\n`prompt.approval_required`; `null` on unrelated events even while\napprovals are pending.",
      "type": [
        "string",
        "null"
      ]
    },
    "correlation_id": {
      "description": "Caller-supplied correlation handle, echoed across the request /\nresponse / event chain it belongs to.",
      "type": [
        "string",
        "null"
      ]
    },
    "monotonic_ns": {
      "description": "Optional process-monotonic counter in nanoseconds, for jitter and\nlatency analysis. Not wall-clock time. Absent when unknown — never\n`null`, so \"unknown\" has exactly one wire spelling.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "payload": {
      "additionalProperties": true,
      "description": "The event's type-specific fields. Shapes for the published types are enforced by the allOf conditionals; unknown fields inside any payload must be ignored.",
      "type": "object"
    },
    "schema_version": {
      "const": 1,
      "description": "Version of the event schema, for the whole event stream. Starts at\n1 and is bumped only on a breaking change (field removed or renamed,\nfield type changed, required field added, event type renamed or\nremoved). Additive growth — new event types, new optional payload\nfields, new namespaces — keeps the version.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "seq": {
      "description": "The canonical ordering primitive: a per-session integer, starting at\n0 on session create, monotonic and gap-free at generation. Ordering\nis by `seq` alone — never by `ts`.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "session_id": {
      "description": "Identifier of the originating session, or `null` for events that are\nnot scoped to one session.",
      "type": [
        "string",
        "null"
      ]
    },
    "ts": {
      "description": "RFC 3339 wall-clock timestamp with millisecond resolution. Not an\nordering key: wall clocks can move backward across corrections.",
      "format": "date-time",
      "type": "string"
    },
    "type": {
      "description": "Namespaced event-type name: dotted and hierarchical, so consumers can subscribe by prefix. The types published in this revision are enumerated in the allOf conditionals; other dotted names are valid — new types arrive within schema_version 1, and consumers must not reject them.",
      "pattern": "^[a-z0-9_]+(\\.[a-z0-9_]+)+$",
      "type": "string"
    }
  },
  "required": [
    "schema_version",
    "session_id",
    "seq",
    "ts",
    "type",
    "payload"
  ],
  "title": "agent-bridge event",
  "type": "object"
}
