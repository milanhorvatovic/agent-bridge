# Captured-session fixture recorded by `interactive-probe record`.
# Generated file — re-recording overwrites it; do not hand-edit.
cli: codex
cli_version: "0.144.6"
install: "npm @openai/codex@0.144.6"
scenario: interrupt
description: "a long generation cut short by the Ctrl+C byte — the interrupt that works across the pinned versions (Esc only interrupts mid-stream on newer ones) — leaving the Conversation-interrupted cell; the session survives"
os: macos
cols: 80
rows: 24
captured_on: 2026-08-03
tier: ci
artifacts:
  input.bytes: 38747
  input.timing.ndjson: 13255
  steps.ndjson: 1273
