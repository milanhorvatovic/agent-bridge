{
  "args": ["-m", "gpt-5.5", "-c", "check_for_update_on_startup=false", "-a", "untrusted"],
  "cli": "codex",
  "description": "one auto-approved command run end to end — ls is on the untrusted policy's trusted list, so it runs with no dialog and lands as the Explored cell",
  "name": "tool-lifecycle",
  "steps": [
    { "label": "trust-dialog", "timeout_ms": 20000, "wait_text": "trust" },
    { "pause_ms": 1000 },
    { "label": "trust-accept", "press": "enter" },
    { "label": "startup-settled", "timeout_ms": 45000, "wait_quiet_ms": 1500 },
    { "label": "tool-prompt", "type_line": "Use the shell to run exactly `ls` and tell me how many entries it prints." },
    { "label": "tool-ran", "timeout_ms": 120000, "wait_text": "Explored" },
    { "label": "turn-end", "timeout_ms": 120000, "wait_quiet_ms": 2500 },
    { "label": "quit-command", "type_line": "/quit" }
  ]
}
