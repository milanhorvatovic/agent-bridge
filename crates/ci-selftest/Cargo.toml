[package]
name = "ci-selftest"
version = "0.0.1"
publish = false
description = "CI-matrix self-test: proves the pinned toolchain builds, tests, and runs on all three target OSes. A placeholder replaced by real probes as the runtime lands."
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "ci-selftest"
path = "src/main.rs"
