[package]
name = "xtask"
version = "0.0.0"
publish = false
description = "Dev-task runner: the single source of truth for the local + CI check sequence. `cargo xtask ci` runs exactly what CI runs."
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[[bin]]
name = "xtask"
path = "src/main.rs"
